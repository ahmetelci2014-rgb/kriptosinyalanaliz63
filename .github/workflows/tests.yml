name: Premium Core Tests

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - "*.py"
      - "requirements.txt"
      - ".github/workflows/tests.yml"

permissions:
  contents: read

concurrency:
  group: premium-core-tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  core-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Repo indir
        uses: actions/checkout@v4

      - name: Python kur
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: requirements.txt

      - name: Bagimliliklari kur
        run: |
          python -m pip install --prefer-binary -r requirements.txt
          python -m pip install "pytest>=8,<10"

      - name: Premium ve Simple Core dosyalarini kontrol et
        run: |
          test -f main.py
          test -f strategy.py
          test -f config.py
          test -f simple_core_strategy.py
          test -f simple_core_runner.py
          test -f premium_core_only_runner.py
          test -f premium_crypto_profit_runner.py
          test -f premium_regime_profit_runner.py
          test -f premium_quality_profit_runner.py
          test -f premium_big_move_profit_runner.py
          test -f premium_big_move_live.py
          test -f premium_market_outlook_refresh.py
          test -f premium_quality_layer.py
          test -f premium_global_quality_guard.py
          test -f premium_profit_runner.py
          test -f premium_early_breakout.py
          test -f premium_regime_transition.py
          test -f position_continuation_shadow.py
          test -f tp3_runner_shadow.py
          test -f multi_day_trend_shadow.py
          test -f big_move_hunter_shadow.py
          test -f big_move_replay_research.py
          test -f big_move_false_positive_shadow.py
          test -f tracking_backfill.py
          test -f source_performance_report.py
          test -f early_breakout_direction_diagnostics.py
          test -f untradable_futures_cleanup.py
          test -f tp3_multi_day_runner_shadow.py
          test -f premium_all_coins.py
          test -f premium_confirmation.py
          test -f premium_continuation.py
          test -f premium_reversal_capture.py
          test -f profitability_engine.py
          test -f portfolio_risk.py
          test -f opportunity_capture.py
          test -f live_entry_safety.py
          test -f coin_analyzer.py
          test -f coin_analysis_runtime.py
          test -f coin_analysis_runtime_v2.py
          test -f movement_start_shadow.py
          test -f movement_start_v2_shadow.py
          test -f movement_start_v3_orderflow_shadow.py
          test -f post_result_shadow.py
          test -f result_diagnostics.py

      - name: Premium ve Simple Core derle
        run: |
          python -m py_compile \
            main.py \
            strategy.py \
            config.py \
            simple_core_strategy.py \
            simple_core_runner.py \
            premium_core_only_runner.py \
            premium_crypto_profit_runner.py \
            premium_regime_profit_runner.py \
            premium_quality_profit_runner.py \
            premium_big_move_profit_runner.py \
            premium_big_move_live.py \
            premium_market_outlook_refresh.py \
            premium_quality_layer.py \
            premium_global_quality_guard.py \
            premium_profit_runner.py \
            premium_early_breakout.py \
            premium_regime_transition.py \
            position_continuation_shadow.py \
            tp3_runner_shadow.py \
            multi_day_trend_shadow.py \
            big_move_hunter_shadow.py \
            big_move_replay_research.py \
            big_move_false_positive_shadow.py \
            tracking_backfill.py \
            source_performance_report.py \
            early_breakout_direction_diagnostics.py \
            untradable_futures_cleanup.py \
            tp3_multi_day_runner_shadow.py \
            premium_all_coins.py \
            premium_confirmation.py \
            premium_continuation.py \
            premium_reversal_capture.py \
            profitability_engine.py \
            portfolio_risk.py \
            opportunity_capture.py \
            live_entry_safety.py \
            coin_analyzer.py \
            coin_analysis_runtime.py \
            coin_analysis_runtime_v2.py \
            movement_start_shadow.py \
            movement_start_v2_shadow.py \
            movement_start_v3_orderflow_shadow.py \
            post_result_shadow.py \
            result_diagnostics.py

      - name: Market Outlook inline refresh regresyonunu calistir
        env:
          TOKEN: ""
          CHAT_ID: ""
        run: python -m pytest -q test_premium_market_outlook_refresh.py

      - name: Big Move regresyonunu calistir
        env:
          TOKEN: ""
          CHAT_ID: ""
        run: python -m pytest -q test_premium_big_move_live.py

      - name: Regime Transition regresyonunu calistir
        env:
          TOKEN: ""
          CHAT_ID: ""
        run: python -m pytest -q test_premium_regime_transition.py

      - name: Mevcut regresyon testlerini calistir
        env:
          TOKEN: ""
          CHAT_ID: ""
        run: |
          for test_file in test_*.py
          do
            echo "Calistiriliyor: ${test_file}"
            python -m pytest -q "${test_file}"
          done
