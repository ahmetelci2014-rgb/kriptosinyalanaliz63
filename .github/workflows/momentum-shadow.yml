name: Momentum Shadow v2

on:
  workflow_dispatch:
  schedule:
    # Maliyet kontrollü gölge örnekleme: 6 saatte bir.
    - cron: "43 1,7,13,19 * * *"

permissions:
  contents: write

concurrency:
  group: momentum-shadow-v2
  cancel-in-progress: false

jobs:
  momentum-shadow:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Repo indir
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Python kur
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: requirements.txt

      - name: Bağımlılıkları kur
        env:
          PIP_DISABLE_PIP_VERSION_CHECK: "1"
        run: |
          python -m pip install --prefer-binary -r requirements.txt

      - name: Momentum V2 motorunu doğrula
        run: |
          python -m py_compile momentum_shadow.py momentum_shadow_v2_runner.py test_momentum_shadow.py
          python momentum_shadow.py --self-test
          python test_momentum_shadow.py -v
          python momentum_shadow_v2_runner.py --self-test

      - name: Momentum V2 gölge analizini çalıştır
        run: python momentum_shadow_v2_runner.py

      - name: Momentum gölge ledgerını kaydet
        if: always()
        run: |
          set -e

          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          if [ -f "momentum_shadow.json" ]; then
            git add momentum_shadow.json
          else
            echo "momentum_shadow.json oluşmadı."
            exit 1
          fi

          if git diff --cached --quiet; then
            echo "Momentum gölge ledgerında yeni değişiklik yok."
            exit 0
          fi

          git commit -m "Update Momentum Shadow V2 ledger"

          for attempt in 1 2 3
          do
            echo "Push denemesi: $attempt"
            git pull --rebase --autostash origin main

            if git push origin HEAD:main; then
              echo "Momentum V2 gölge ledgerı kaydedildi."
              exit 0
            fi

            sleep 10
          done

          echo "Momentum V2 gölge ledgerı üç denemede de kaydedilemedi."
          exit 1
